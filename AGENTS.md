# Agents

- Rules: `.cursor/rules/` (memory MCP, token discipline, luxury-rich-compact UI)
- MCP: `.cursor/mcp.json` (codebase-memory) — graph project name: `fs-c5-app`
- Prefer graph tools over broad scans; keep context minimal.
