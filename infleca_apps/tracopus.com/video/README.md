# Product walkthrough video

Place the Tracopus demo video here (from the legacy site `video/demo.mpg`):

- `demo.mpg` (primary)
- `demo.mp4` (optional fallback)
- `demo.webm` (optional fallback)

The homepage video section loads these paths automatically.
